function convert1x (fahr integer) [* comment *]
{
    ret 5*(fahr - 32)/ 9;
}
#
    integer low, high, step;
    get (low, high, step);
    while (low <= high )
    { put (low);
        put (convert1x (low));
        low = low + step;
    }
    5*()
#
